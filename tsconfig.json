{
    "compilerOptions": {
      "outDir": "./dist/", // куда компилировать
      "sourceMap": true, // Создать source map файлы для готовых файлов JavaScript (хороошо подходит для дебаггинга)
      "removeComments": true, //Игнорировать комментарии
      "noImplicitAny": true, // Будет выводиться ошибка , когда отсутствуют аннотации типов
      "module": "esnext",
      "target": "es6",
      "allowJs": true,     // Позволяет компилировать JS-файлы
      "checkJs": false,     // true- Проверяет типы в JavaScript-файлах и сообщает об ошибках , false- если я хочу постепенно переходить на ts, но на типы js файлы не проверяются 
      "moduleResolution": "node",
      "alwaysStrict": true,               //Гарантирует, что ваши файлы анализируются в строгом режиме ECMAScript, и выдает «use strict» для каждого исходного файла.
    "strictNullChecks": true,             // Используется в паре с strictPropertyInitialization
    "strictPropertyInitialization": true, //Если установлено значение true, TypeScript выдаст ошибку, если свойство класса было объявлено, но не задано в конструкторе. 
    //"allowSyntheticDefaultImports": true, //Разрешить синтетический импорт по умолчанию import React from "react";
    //"esModuleInterop": true
    //"forceConsistentCasingInFileNames": true //
    },
    "include": ["src"] //Компилировать только файлы из папки src
  }